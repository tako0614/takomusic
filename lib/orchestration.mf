export fn piccolo() {
  return { name: "piccolo", family: "woodwind", range: { low: 74, high: 102 }, transposition: 12, clef: "treble" };
}

export fn flute() {
  return { name: "flute", family: "woodwind", range: { low: 60, high: 96 }, transposition: 0, clef: "treble" };
}

export fn altoFlute() {
  return { name: "altoFlute", family: "woodwind", range: { low: 55, high: 91 }, transposition: -5, clef: "treble" };
}

export fn oboe() {
  return { name: "oboe", family: "woodwind", range: { low: 58, high: 91 }, transposition: 0, clef: "treble" };
}

export fn englishHorn() {
  return { name: "englishHorn", family: "woodwind", range: { low: 52, high: 81 }, transposition: -7, clef: "treble" };
}

export fn clarinet() {
  return { name: "clarinet", family: "woodwind", range: { low: 50, high: 94 }, transposition: -2, clef: "treble" };
}

export fn clarinetA() {
  return { name: "clarinetA", family: "woodwind", range: { low: 50, high: 94 }, transposition: -3, clef: "treble" };
}

export fn bassClarinet() {
  return { name: "bassClarinet", family: "woodwind", range: { low: 38, high: 77 }, transposition: -14, clef: "treble" };
}

export fn bassoon() {
  return { name: "bassoon", family: "woodwind", range: { low: 34, high: 75 }, transposition: 0, clef: "bass" };
}

export fn contrabassoon() {
  return { name: "contrabassoon", family: "woodwind", range: { low: 22, high: 58 }, transposition: -12, clef: "bass" };
}

export fn horn() {
  return { name: "horn", family: "brass", range: { low: 34, high: 77 }, transposition: -7, clef: "treble" };
}

export fn trumpet() {
  return { name: "trumpet", family: "brass", range: { low: 55, high: 82 }, transposition: -2, clef: "treble" };
}

export fn trumpetC() {
  return { name: "trumpetC", family: "brass", range: { low: 55, high: 82 }, transposition: 0, clef: "treble" };
}

export fn trombone() {
  return { name: "trombone", family: "brass", range: { low: 40, high: 72 }, transposition: 0, clef: "bass" };
}

export fn bassTrombone() {
  return { name: "bassTrombone", family: "brass", range: { low: 34, high: 67 }, transposition: 0, clef: "bass" };
}

export fn tuba() {
  return { name: "tuba", family: "brass", range: { low: 28, high: 58 }, transposition: 0, clef: "bass" };
}

export fn violin() {
  return { name: "violin", family: "strings", range: { low: 55, high: 103 }, transposition: 0, clef: "treble", openStrings: [55, 62, 69, 76] };
}

export fn viola() {
  return { name: "viola", family: "strings", range: { low: 48, high: 93 }, transposition: 0, clef: "alto", openStrings: [48, 55, 62, 69] };
}

export fn cello() {
  return { name: "cello", family: "strings", range: { low: 36, high: 84 }, transposition: 0, clef: "bass", openStrings: [36, 43, 50, 57] };
}

export fn contrabass() {
  return { name: "contrabass", family: "strings", range: { low: 28, high: 67 }, transposition: -12, clef: "bass", openStrings: [28, 33, 38, 43] };
}

export fn harp() {
  return { name: "harp", family: "strings", range: { low: 24, high: 103 }, transposition: 0, clef: "grand" };
}

export fn timpani() {
  return { name: "timpani", family: "percussion", range: { low: 40, high: 55 }, transposition: 0, clef: "bass" };
}

export fn xylophone() {
  return { name: "xylophone", family: "percussion", range: { low: 65, high: 108 }, transposition: 12, clef: "treble" };
}

export fn marimba() {
  return { name: "marimba", family: "percussion", range: { low: 45, high: 96 }, transposition: 0, clef: "grand" };
}

export fn vibraphone() {
  return { name: "vibraphone", family: "percussion", range: { low: 53, high: 89 }, transposition: 0, clef: "treble" };
}

export fn glockenspiel() {
  return { name: "glockenspiel", family: "percussion", range: { low: 79, high: 108 }, transposition: 24, clef: "treble" };
}

export fn celesta() {
  return { name: "celesta", family: "percussion", range: { low: 60, high: 108 }, transposition: 12, clef: "grand" };
}

export fn tubularBells() {
  return { name: "tubularBells", family: "percussion", range: { low: 60, high: 77 }, transposition: 0, clef: "treble" };
}

export fn piano() {
  return { name: "piano", family: "keyboard", range: { low: 21, high: 108 }, transposition: 0, clef: "grand" };
}

export fn organ() {
  return { name: "organ", family: "keyboard", range: { low: 36, high: 96 }, pedalRange: { low: 24, high: 55 }, transposition: 0, clef: "grand" };
}

export fn isInRange(instrument, pitch) {
  return pitch >= instrument.range.low && pitch <= instrument.range.high;
}

export fn toConcertPitch(instrument, writtenPitch) {
  return writtenPitch + instrument.transposition;
}

export fn toWrittenPitch(instrument, concertPitch) {
  return concertPitch - instrument.transposition;
}

export fn isChordPlayable(instrument, chord) {
  for (pitch in chord) {
    if (!isInRange(instrument, pitch)) {
      return false;
    }
  }
  return true;
}

export fn woodwindSection() {
  return { name: "woodwinds", instruments: [{ instrument: piccolo(), count: 1 }, { instrument: flute(), count: 2 }, { instrument: oboe(), count: 2 }, { instrument: englishHorn(), count: 1 }, { instrument: clarinet(), count: 2 }, { instrument: bassClarinet(), count: 1 }, { instrument: bassoon(), count: 2 }, { instrument: contrabassoon(), count: 1 }] };
}

export fn brassSection() {
  return { name: "brass", instruments: [{ instrument: horn(), count: 4 }, { instrument: trumpet(), count: 3 }, { instrument: trombone(), count: 2 }, { instrument: bassTrombone(), count: 1 }, { instrument: tuba(), count: 1 }] };
}

export fn stringSection() {
  return { name: "strings", instruments: [{ instrument: violin(), count: 16, divisi: "I/II" }, { instrument: viola(), count: 12 }, { instrument: cello(), count: 10 }, { instrument: contrabass(), count: 8 }, { instrument: harp(), count: 2 }] };
}

export fn percussionSection() {
  return { name: "percussion", instruments: [{ instrument: timpani(), count: 4 }, { instrument: xylophone(), count: 1 }, { instrument: marimba(), count: 1 }, { instrument: vibraphone(), count: 1 }, { instrument: glockenspiel(), count: 1 }, { instrument: tubularBells(), count: 1 }] };
}

export fn symphonyOrchestra() {
  return { name: "symphonyOrchestra", sections: [woodwindSection(), brassSection(), stringSection(), percussionSection()] };
}

export fn chamberOrchestra() {
  return { name: "chamberOrchestra", instruments: [{ instrument: flute(), count: 1 }, { instrument: oboe(), count: 1 }, { instrument: clarinet(), count: 1 }, { instrument: bassoon(), count: 1 }, { instrument: horn(), count: 2 }, { instrument: violin(), count: 8, divisi: "I/II" }, { instrument: viola(), count: 4 }, { instrument: cello(), count: 4 }, { instrument: contrabass(), count: 2 }] };
}

export fn stringQuartet() {
  return { name: "stringQuartet", instruments: [{ instrument: violin(), part: "Violin I" }, { instrument: violin(), part: "Violin II" }, { instrument: viola(), part: "Viola" }, { instrument: cello(), part: "Cello" }] };
}

export fn windQuintet() {
  return { name: "windQuintet", instruments: [{ instrument: flute(), part: "Flute" }, { instrument: oboe(), part: "Oboe" }, { instrument: clarinet(), part: "Clarinet" }, { instrument: horn(), part: "Horn" }, { instrument: bassoon(), part: "Bassoon" }] };
}

export fn brassQuintet() {
  return { name: "brassQuintet", instruments: [{ instrument: trumpet(), part: "Trumpet I" }, { instrument: trumpet(), part: "Trumpet II" }, { instrument: horn(), part: "Horn" }, { instrument: trombone(), part: "Trombone" }, { instrument: tuba(), part: "Tuba" }] };
}

export fn pianoTrio() {
  return { name: "pianoTrio", instruments: [{ instrument: piano(), part: "Piano" }, { instrument: violin(), part: "Violin" }, { instrument: cello(), part: "Cello" }] };
}

export fn doubling(primaryInstrument, doublingInstrument, intervalOffset) {
  let offset = intervalOffset;
  if (offset == null) {
    offset = 0;
  }
  return { type: "doubling", primary: primaryInstrument, doubling: doublingInstrument, interval: offset };
}

export fn fluteOboUnison() {
  return doubling(flute(), oboe(), 0);
}

export fn fluteViolinOctave() {
  return doubling(flute(), violin(), 0);
}

export fn celloContrabassOctave() {
  return doubling(cello(), contrabass(), 12);
}

export fn trumpetHornUnison() {
  return doubling(trumpet(), horn(), 0);
}

export fn tuttiUnison(instruments, pitches) {
  let parts = [];
  for (inst in instruments) {
    parts[parts.length] = { instrument: inst, pitches: pitches };
  }
  return { type: "tutti", parts: parts };
}

export fn distributeChord(chord, instruments) {
  let assignment = [];
  const numPitches = chord.length;
  const numInstruments = instruments.length;
  for (i in 0 .. numPitches - 1) {
    const instIndex = i % numInstruments;
    const inst = instruments[instIndex];
    const pitch = chord[i];
    if (isInRange(inst, pitch)) {
      assignment[assignment.length] = { instrument: inst, pitch: pitch };
    }
  }
  return assignment;
}

export fn closeVoicing(chord, topPitch) {
  let voiced = [];
  let current = topPitch;
  let sorted = sortDescending(chord);
  for (pc in sorted) {
    voiced[voiced.length] = current;
    current = current - 1;
  }
  return voiced;
}

fn sortDescending(arr) {
  let result = [];
  for (a in arr) {
    result[result.length] = a;
  }
  for (i in 0 .. result.length - 2) {
    for (j in 0 .. result.length - i - 2) {
      if (result[j] < result[j + 1]) {
        const temp = result[j];
        result[j] = result[j + 1];
        result[j + 1] = temp;
      }
    }
  }
  return result;
}

export fn openVoicing(chord, bassPitch, soprano) {
  let voiced = [];
  voiced[0] = bassPitch;
  let current = bassPitch + 12;
  for (i in 1 .. chord.length - 1) {
    const pc = chord[i] % 12;
    voiced[voiced.length] = current;
    current = current + 3;
  }
  return voiced;
}

export fn dynamicWeight(instrument) {
  if (instrument.family == "brass") {
    return 1.5;
  } else if (instrument.family == "percussion") {
    return 1.3;
  } else if (instrument.family == "woodwind") {
    return 1;
  } else if (instrument.family == "strings") {
    return 0.8;
  }
  return 1;
}

export fn balanceCheck(assignments) {
  let totalWeight = 0;
  let familyWeights = {};
  for (a in assignments) {
    const weight = dynamicWeight(a.instrument);
    totalWeight = totalWeight + weight;
    const family = a.instrument.family;
    if (familyWeights[family] == null) {
      familyWeights[family] = 0;
    }
    familyWeights[family] = familyWeights[family] + weight;
  }
  return { total: totalWeight, byFamily: familyWeights };
}

export const ARCO = "arco";

export const PIZZICATO = "pizz";

export const COL_LEGNO = "colLegno";

export const SUL_TASTO = "sulTasto";

export const SUL_PONTICELLO = "sulPont";

export const TREMOLO = "tremolo";

export const HARMONICS = "harmonics";

export const MUTE = "mute";

export const DIVISI = "divisi";

export const OPEN = "open";

export const STOPPED = "stopped";

export const MUTED = "muted";

export const CUIVRE = "cuivre";

export const FLUTTER = "flutter";

export const LEGATO = "legato";

export const STACCATO = "staccato";

export const FLUTTER_TONGUE = "flutterTongue";

export const DOUBLE_TONGUE = "doubleTongue";

export const SLAP_TONGUE = "slapTongue";

export const MULTIPHONIC = "multiphonic";

export fn withTechnique(part, technique) {
  return { part: part, technique: technique };
}

export fn getRegister(instrument, pitch) {
  const range = instrument.range;
  const span = range.high - range.low;
  const position = pitch - range.low;
  const percent = position / span;
  if (percent < 0.2) {
    return "low";
  } else if (percent < 0.4) {
    return "lowMid";
  } else if (percent < 0.6) {
    return "mid";
  } else if (percent < 0.8) {
    return "highMid";
  } else {
    return "high";
  }
}

export fn tessitura(instrument) {
  const range = instrument.range;
  const span = range.high - range.low;
  return { low: range.low + span * 0.2, high: range.high - span * 0.15 };
}

export fn isInTessitura(instrument, pitches) {
  const tess = tessitura(instrument);
  for (p in pitches) {
    if (p < tess.low || p > tess.high) {
      return false;
    }
  }
  return true;
}

export fn scoreOrder() {
  return ["piccolo", "flute", "altoFlute", "oboe", "englishHorn", "clarinet", "clarinetA", "bassClarinet", "bassoon", "contrabassoon", "horn", "trumpet", "trumpetC", "trombone", "bassTrombone", "tuba", "timpani", "percussion", "celesta", "piano", "organ", "harp", "violin", "viola", "cello", "contrabass"];
}

export fn sortByScoreOrder(instruments) {
  const order = scoreOrder();
  let result = [];
  for (name in order) {
    for (inst in instruments) {
      if (inst.name == name) {
        result[result.length] = inst;
      }
    }
  }
  return result;
}

export fn cue(targetInstrument, sourceInstrument, pitches) {
  return { type: "cue", target: targetInstrument, source: sourceInstrument, pitches: pitches, size: "small" };
}

export fn getAllInstruments(ensemble) {
  let result = [];
  if (ensemble.sections != null) {
    for (section in ensemble.sections) {
      for (entry in section.instruments) {
        result[result.length] = entry.instrument;
      }
    }
  } else if (ensemble.instruments != null) {
    for (entry in ensemble.instruments) {
      result[result.length] = entry.instrument;
    }
  }
  return result;
}

export fn findInstrumentsForPitch(ensemble, pitch) {
  let suitable = [];
  const instruments = getAllInstruments(ensemble);
  for (inst in instruments) {
    if (isInRange(inst, pitch)) {
      suitable[suitable.length] = inst;
    }
  }
  return suitable;
}

export fn ensembleRange(ensemble) {
  const instruments = getAllInstruments(ensemble);
  let lowest = 127;
  let highest = 0;
  for (inst in instruments) {
    const concertLow = toConcertPitch(inst, inst.range.low);
    const concertHigh = toConcertPitch(inst, inst.range.high);
    if (concertLow < lowest) {
      lowest = concertLow;
    }
    if (concertHigh > highest) {
      highest = concertHigh;
    }
  }
  return { lowest: lowest, highest: highest, span: highest - lowest };
}
